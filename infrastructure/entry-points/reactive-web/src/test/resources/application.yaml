# infrastructure/entry-points/reactive-web/src/test/resources/application.yaml
routes:
  paths:
    loanApplication: "/api/v1/solicitud"

cors:
  allowed-origins: "*"

# Configuración para tests
spring:
  main:
    web-application-type: reactive